package utils

/*
import (
	"fmt"
	"strings"

	"github.com/grokify/go-ringcentral-engage/engagedigital"
)

const DefaultTopologyName string = "Default Topology"

func FindDefaultTopology(tops []engagedigital.Topology) (engagedigital.Topology, error) {
	topNames := []string{}
	for _, top := range tops {
		if top.Name == DefaultTopologyName {
			return top, nil
		} else {
			topNames = append(topNames, top.Name)
		}
	}
	return engagedigital.Topology{}, fmt.Errorf("E_DEFAULT_TOPOLOGY_NOT_FOUND default [%s] found [%s]",
		DefaultTopologyName, strings.Join(topNames, ","))
}
*/
